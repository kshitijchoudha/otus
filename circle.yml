machine:
  node:
    version: 6.9.0
dependencies:
  override:
    - npm install
    - npm install webpack -g
    - webpack
  pre:
    - ./buildui.sh
general:
  artifacts:
    - "otus-ui/target/otus-ui-1.0-SNAPSHOT.jar"
